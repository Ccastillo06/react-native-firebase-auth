import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  TitleText: {
    fontSize: 20,
    fontWeight: 'bold',
  },
  SubtitleText: {
    fontSize: 16,
    marginTop: 16,
    marginBottom: 16,
  },
  ErrorMessage: {
    color: 'red',
    textAlign: 'center',
  },
  GoogleButton: {
    width: 192,
    height: 48,
  },
});

export default styles;
